# Agenda KPI · Lista completa

- El contador y el listado de sesiones pendientes consumen el mismo array de `pmSessionAgenda()`.
- Se elimina el límite visual de cuatro filas.
- Todas las sesiones pendientes se renderizan dentro del contenedor con scroll.
- La misma regla se aplica a las sesiones terminadas.
- Se incrementa la versión de caché de la PWA.
